To lunch the app follow the steps : 

1/ insall all the required packages mentioned on the reqirements.txt (using pip install command)
2/execute this commande if you are on wind to lunch the server python "main.py" 
3/ Open the IP address displayed on any browser to see the web app 